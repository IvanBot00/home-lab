services:
  traefik:
    image: traefik:v3.3
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - ./traefik.yml:/traefik.yml:ro
      - ./config:/config:ro
